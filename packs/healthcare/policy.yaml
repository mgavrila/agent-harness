# Default action-class policy for a healthcare credentialing deployment.
# A client overrides it in its own document's policy section; see
# harness/core-tools/src/domain/tooling/policy.ts for how the two are merged.
classes:
  # Reading records, deadlines, the audit log, and public registry lookups.
  read: auto
  # Writing to our own Postgres. Reversible and fully audited.
  write.internal: auto
  # Anything that leaves the building: a roster to a payer, a file released to a human.
  external: approval
  # Moving money. Not part of credentialing at all.
  financial: blocked
  # Deleting or overwriting a record beyond repair.
  destructive: approval
